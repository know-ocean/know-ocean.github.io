source "https://rubygems.org"

# Use the same environment GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

# Needed for Ruby 3+ (local dev only)
gem "webrick"

# Optional: any other local-only gems go here
# gem "jekyll-admin"ruby 